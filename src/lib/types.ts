// Shared type definitions. Append-only — each ticket adds its types under a fence.
// === T01 ===
// (placeholder so the file isn't empty; T02 fills this in)
export type ISODate = string;
// === /T01 ===
