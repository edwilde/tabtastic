# Tabtastic!

*Window Time Machine for Chrome.*

Save and restore your Chrome project windows — tab groups, colors, names, and all.

## Development

```sh
npm install
npm run dev        # vite (HMR for popup/options)
npm run build      # production build → dist/
npm test           # vitest
npm run typecheck  # tsc --noEmit
```

## Loading the unpacked extension

1. `npm run build`
2. Open `chrome://extensions`, enable Developer mode, click **Load unpacked**, and select `dist/`.

## Project layout

- `src/manifest.ts` — Manifest V3, generated by `@crxjs/vite-plugin`.
- `src/background/` — service worker shell + per-feature `handlers/` and `listeners/`.
- `src/lib/` — pure modules (types, storage, capture, restore, retention, etc.).
- `src/popup/`, `src/options/` — UI surfaces.
- `tests/` — Vitest unit tests; `tests/setup/chrome-stub.ts` provides a Proxy stub for `globalThis.chrome` so module loads don't throw.

## Backlog

See [`TICKETS.md`](./TICKETS.md). Design at [`docs/plans/2026-05-01-chrome-project-snapshots-design.md`](./docs/plans/2026-05-01-chrome-project-snapshots-design.md).
